name: Build containers and push to registery

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Frontend image tags
        id: frontend
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/frontend
          tags: |
            type=raw,value=latest

      - name: Build and Push Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ steps.frontend.outputs.tags }}
          labels: ${{ steps.frontend.outputs.labels }}

  build-api:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Backend api image tags
        id: backend-api
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/api
          tags: |
            type=raw,value=latest

      - name: Build and Push Backend api Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend/api
          file: ./backend/api/Dockerfile
          push: true
          tags: ${{ steps.backend-api.outputs.tags }}
          labels: ${{ steps.backend-api.outputs.labels }}

  build-db:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Database image tags
        id: db
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/db
          tags: |
            type=raw,value=latest

      - name: Build and Push Database Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend/db
          file: ./backend/db/Dockerfile
          push: true
          tags: ${{ steps.db.outputs.tags }}
          labels: ${{ steps.db.outputs.labels }}

  build-proxy:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Proxy image tags
        id: proxy
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/proxy
          tags: |
            type=raw,value=latest

      - name: Build and Push Proxy Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend/proxy
          file: ./backend/proxy/Dockerfile
          push: true
          tags: ${{ steps.proxy.outputs.tags }}
          labels: ${{ steps.proxy.outputs.labels }}
